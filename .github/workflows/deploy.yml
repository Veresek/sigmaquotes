name: Deployment

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Create .env files from Secrets
              uses: appleboy/ssh-action@v1.2.4
              with:
                  host: ${{ secrets.DEPLOY_HOST }}
                  username: ${{ secrets.DEPLOY_USER }}
                  key: ${{ secrets.DEPLOY_KEY }}
                  port: ${{ secrets.DEPLOY_PORT }}
                  script: |
                      cd ./sigmaquotes
                      
                      cat << 'EOF' > .env
                      ${{ secrets.MAIN_ENV_FILE }}
                      EOF
                      
                      mkdir -p bots
                      cat << 'EOF' > bots/.env
                      ${{ secrets.BOTS_ENV_FILE }}
                      EOF

            - name: Deploy containers via Docker Compose
              uses: appleboy/ssh-action@v1.2.4
              with:
                  host: ${{ secrets.DEPLOY_HOST }}
                  username: ${{ secrets.DEPLOY_USER }}
                  key: ${{ secrets.DEPLOY_KEY }}
                  port: ${{ secrets.DEPLOY_PORT }}
                  script: |
                      cd ./sigmaquotes
                      git pull origin main
                      sudo docker compose up -d --build

            - name: Restart Python Bot via PM2
              uses: appleboy/ssh-action@v1.2.4
              with:
                  host: ${{ secrets.DEPLOY_HOST }}
                  username: ${{ secrets.DEPLOY_USER }}
                  key: ${{ secrets.DEPLOY_KEY }}
                  port: ${{ secrets.DEPLOY_PORT }}
                  script: |
                      cd ./sigmaquotes/bots
                      pm2 restart sigmaquotes.py --name "sigmaquotes" || pm2 start sigmaquotes.py --name "sigmaquotes" --interpreter python3
                      pm2 save
